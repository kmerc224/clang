name: Publish AOSP Clang

on:
  workflow_dispatch: # 手动触发

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 必须赋予写入权限才能创建 Release

    steps:
      - name: Download AOSP Clang
        run: |
          # 下载文件
          wget https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/android15-release/clang-r498229b.tar.gz -O clang-r498229b.tar.gz
          
          # 简单校验一下文件是否下载成功
          if [ ! -f clang-r498229b.tar.gz ]; then
            echo "Error: Download failed"
            exit 1
          fi
          echo "File downloaded successfully."

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: clang-r498229b
          name: AOSP Clang r498229b
          body: "Automatically downloaded AOSP Clang r498229b from Android source."
          files: clang-r498229b.tar.gz
          draft: false
          prerelease: false
